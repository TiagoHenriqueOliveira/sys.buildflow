<button type="button"
    class="btn btn-sm btn-indigo btn-modal-tipo-atendimento"
    data-id="{{ $t->tp_aten_id }}"
    data-descricao="{{ e($t->tp_aten_descricao) }}"
    data-ativo="{{ $t->tp_aten_ativo }}">
    <i class="fas fa-edit"></i>
</button>